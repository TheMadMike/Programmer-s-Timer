const app = new Vue({
    el: '#root',

    data: {
        showMobileButtons: false,
    },

    methods: {
        showPatternsMenu: () => {
        },

        showCustomizationMenu: () => {
        },

        start: () => {
            startTimer();
        }
    }
});